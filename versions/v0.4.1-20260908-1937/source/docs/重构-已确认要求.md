# 当前重构要求（2026-09-08）

这是项目内记录，不是全局记忆。继续工作先读本文件、《重构-LibTV交互对照表》《重构-问题与验收表》以及最新用户消息。

- 用户报告 v0.4.0 正式软件本地卡片和工具普遍失败，原生映射打不开；旧测试数量不能代表用户环境可用。
- 顺序：冻结与定位公共执行故障 → 完整观察 LibTV → 确认代表性交互样板和独有功能位置 → 一条正式 EXE 端到端流程 → 扩展所有入口。
- 交互以 LibTV 实际操作为准：卡片布局、按钮位置、菜单、选择、编辑、拖动、连接、快捷键和状态变化。未观察的不得凭空补成“复刻”。独有功能的位置询问用户。
- 最新用户确认：本地工作流与编辑映射应始终可见，不收进模型菜单。选中本地卡片时直接展示工作流入口和编辑映射；工具参数可以放在功能标签的展开区。
- 映射必须在编辑映射里看到对应的真实 ComfyUI 工作流，可选节点、参数、输入输出，公开控件并预览卡片；不是一个外链按钮。DX OS 只参考方式。
- 可替换现有画布架构；保护工程格式、用户资产、可复用服务及可用的 Codex/Image2，不同时盲目重写全部模块。
- 新 LibTV 测试画布：https://www.liblib.tv/canvas?guideSource=home-feature-grid&spaceId=7877720&projectId=fbd1ef51bcd94e6ca11393583dbaeb05 。用户允许测试和非视频生成，预算现有积分，视频生成须另问；旧画布不再用作测试。
- 验收素材在 `C:/Users/O.oInkMore/Desktop/CreateMore/AAA`；只读原素材，结果写独立验收工程。不得改用户正在使用的项目。
- 用户同时使用电脑；先用独立网页控制，不擅自切换桌面鼠标操作。需要桌面操作时协调时段。
- 现有 ComfyUI/共享模型继续使用，累计新增预算 100 GB（上一轮权重约 40.38 GB），不得另装 ComfyUI 或降级主环境。
- 正式 EXE + 实际用户连接 + 实际素材 + 真实结果 + 保存重开，是最终验收条件；不能用测试端口或模拟生成代替。
