# LibTV 交互补测（2026-09-08）

## 本轮实际完成

- 用户授权刷新指定 Edge 画布。刷新成功后实际等待 120 秒，再读取画面。
- 已加载项目：神谕者- 副本；界面显示 183 个节点、94 积分、12% 缩放。
- 点击侧栏“定位到节点 CM结构测试-图片”：定位并选中测试图片卡片，缩放变为 100%，下方显示生成参数面板。不是单纯读取布局。
- 点击“16:9 · 2K · 1张”展开设置：分辨率 1K/2K/4K；比例含自适应、1:1、9:16、16:9、3:4、4:3、3:2、2:3、4:5、5:4、21:9；数量 1/2/4 张。
- 点击 9:16：空图片卡片立即变成竖版。本次视口中下方参数面板随卡片变高移到屏幕外。需要在我们的实现中处理面板可见性，而不是照搬此边界行为。
- 打开快捷键面板，读取页面实际说明：Ctrl/Alt+G 成组；Ctrl+Alt+G 合并分镜组；Ctrl/Alt+Shift+G 解组；Ctrl+L 连线；Ctrl+D 复制节点和连线；Ctrl+Enter 生成；Tab 新建节点；Alt+拖动复制节点；Ctrl+Alt+拖动创建副本；Ctrl+0 适应画布；Space 移动画布；V 移动；H 抓手；Alt+Shift+F 整理；Ctrl+Z 撤销；Ctrl+Shift+Z 重做；Ctrl+F 节点搜索。以上仅为说明已读取，不代表手势已逐项运行。

## 改动与边界

- 仅将已有测试节点“CM结构测试-图片”的比例改为 9:16；未生成图片或视频，未删除节点，未改动正式素材。
- 未验证：鼠标拖拽、端口拖线、框选、分组、删除连线、撤销恢复、比例刷新后持久化。
- 后续鼠标连接再次报告 Debugger unattached。重新获取标签、读取画面、尝试刷新仍失败。不要将连接成功过等同于全套交互验证完成。
- 后续继续时先恢复同一用户标签的连接，保留正式节点；不要因连接失败改用隐藏页面接口代替实际交互测试。

## 恢复连接后的实际鼠标补测

- Edge 扩展连接恢复，继续使用同一项目标签，没有刷新或关闭其他页面。
- 点击侧栏定位“CM结构测试-图片 - 副本”，用鼠标从卡片内部向左拖动；截图确认卡片移至左侧空白区，下面的参数面板同步跟随，比例、模型和数量保留。
- 用鼠标从该副本右侧输出端口拖至左上空白处，松开后出现“引用该节点生成”菜单；存在临时节点和临时连接，计数短暂从 183 变成 184。
- 在落点菜单点击“文本”，临时连接替换为真实连接：`i-s35tO1ybr9 → t-guudoZr450`。文本卡片显示 1 个引用、默认提示词“根据图片生成提示词”、模型 GVLM 3.1 和 6 积分生成提示。没有点击生成。
- 从侧栏更多菜单进入重命名，输入“CM手势测试-拖线创建文本”并按回车，已回读确认画布标题和侧栏名称均更新。
- 再次鼠标拖动新文本卡片向左，参数面板中的引用数量和提示词保留。未检查中间每一帧的连线动画，不据此声称动画性能已验证。
- 截图显示：向左拖线创建时，新文本卡片与原图片卡片重叠。实现时要兼顾落点直觉、空位放置和生成面板可见性。

### 本轮遗留状态

- 新增 1 个有明确测试名称的空文本节点和 1 条测试连接，总节点数 184；移动了图片测试副本和新文本测试节点。
- 未生成图片或视频，未删除节点或修改正式素材。
- 仍待实测：已有端口之间直接连线、取消未完成连线、框选/分组/解组、复制及撤销重做、删除连线、刷新后位置与参数持久化；完整生成工具链另行验证。
