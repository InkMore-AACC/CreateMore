# LibTV 交互对照表

> 2026-09-09 补充：此前“需要处理面板可见性”的判断已被用户明确否决。最新实操确认 LibTV 允许工具栏和生成框超出当前视口，CreateMore 同样保持卡片相对位置，不自动换边或移动画布。

状态只能逐项推进：尚未观察 → 已观察 → 已确认设计 → 已实现 → 正式软件验收通过。

2026-09-08 新画布初次页面读取成功，显示剩余 94 积分。页面已有一个图片节点，两侧工作区和助手面板展开。关闭助手后合并读取超时，恢复读取确认助手已关闭。之后动作与读取分开，添加文本和进入手动编辑均成功。本轮尚未生成，积分未消费。

## 已实际操作的状态

- 添加节点菜单：文本、图片、视频、智能剪辑 Beta、导演台、逐帧拉片 SD 2.5、音频、脚本、素材库；添加资源区有上传、从生成历史选择。不是全部平铺在画布主工具栏。
- 添加文本后选中该节点，左侧节点列表高亮，并显示总计 2 节点；画布新增位置在已有图片节点附近，有重叠，不能假设自动避让。
- 空文本卡片：标题位于卡片上方，左右连接点；卡片内有“自己编写内容、文生视频、图片反推提示词、文字生音乐”尝试入口。
- AI 输入区域不是塞在小卡片内，而是卡片下方一块较宽的浮动面板；模型选择在左下，翻译/积分/提交在右下，右上有展开图标。已查看截图，当前画布缩放 50%。
- 点击“自己编写内容”后，尝试入口和 AI 输入面板消失；出现背景色、标题 1/2/3、正文、粗体、斜体、无序/有序列表、分割线、复制内容、展开编辑以及“输入内容…”编辑区。此状态切换已回读，不等同于文字输入保存测试。
- 手动输入“交互验收：一位摄影师把陶杯放在窗边。”后，卡片文本已回读确认。点击展开编辑出现标题、关闭（ESC）和富文本工具栏；展开区的可访问树仍显示输入占位而非已输入文字，暂记待核对，不把这当成设计要求照搬。已按 Esc，待回读是否关闭。

| 范围 | 当前证据 | 状态 |
|---|---|---|
| 总体布局 | 已查看全屏截图：顶左项目/画布，顶右账号/Agent，底中工具栏，底左视图控制 | 已观察主要布局 |
| 卡片内部排版/选中编辑 | 图片/文本/视频编辑状态已读取，图片浮动面板截图已查看 | 部分观察 |
| 工具菜单/结果呈现 | 全局工具箱为带封面和使用按钮的预设库；图生图入口新增并连接结果节点 | 部分观察 |
| 拖动、框选、缩放、连线、快捷键 | 已拖动图生图节点；Tab 打开新建菜单成功；快捷键列表已读取，框选连线仍需补测 | 部分观察 |
| 图片/文字生成及失败状态 | 已实提并取得 2048×2048 图片；中间等待动画和失败状态尚未捕获 | 图片成功回填已观察 |
| 视频 | 不提交视频生成；静态控件可观察 | 未生成 |
| 本地 ComfyUI/映射等独有入口 | 不猜测 LibTV 对应位置，观察后集中列差异给用户确认 | 待确认设计 |

## 2026-09-09 全卡片复核

- 普通图片按源图比例显示，没有下方生成框；顶部完整排列人像质感调节、全景、多角度、打光、九宫格、高清、元素编辑、图层分离、宫格切分、下载、展开，不折叠到“更多”。
- 普通视频按 1024×576 的 16:9 比例显示，没有下方生成框；顶部为高清、片段重拍、逐帧拉片、智能去字幕、音视频分离、主体消除、截取当前帧、下载、展开。
- 生成图片空卡下方为参考、标记、风格、聚焦、扩大；有结果时输入素材以编号缩略图显示，提示词含 `@` 引用。规格菜单实际包含多种比例、1／2／4 张；CreateMore 按用户要求另外保留 3K 和自定义宽高。
- 生成视频框顶部为参考、标记、特效、角色库、运镜、扩大；底部依次为模型、生成模式、比例／质量／时长／数量及生成。没有进行视频生成。
- 生成文本框主要为提示词、模型与生成，没有照搬图片参考按钮；生成声音框顶部只有参考与扩大，另有语言／采样率／格式和速度、音调、音量。
- 在空白处通过新建菜单创建图片卡片后，卡片落在操作位置附近，画布没有自动跳转居中；选中靠边卡片时，工具栏和生成框直接超出视口。

## 本轮补测记录

- 文本展开后 Esc 没有关闭编辑器，反而收起了底层侧栏；点击右上关闭后卡片变为空提示。属于观察到的异常，不作为复刻目标。
- 图片节点“图生图”入口创建新的图生图节点并自动连到源图片；原图片保留。编辑器位于新节点下方，宽度明显大于卡片，参考区显示编号缩略图。
- 图片编辑器顶部：参考、标记、风格；底部：模型、集中输出设置、预设、提交及积分；还有 AutoLink。点击输出设置弹出画质/清晰度/比例/张数；切换 1:1 后，标准画质、2K、1 张保持不变。未提交生成，积分仍 94。
- 真实拖动新图生图节点标题后，节点编辑区域保持可用；需再截图核对跟随及屏幕边界策略。
- 工具箱不是通用编辑按钮列表，而是预设资源卡片，包含封面、名称、使用按钮及“我的工具箱”。尚未点击使用视频预设。
- 快捷键列表显示 Ctrl+D 复制节点与连线、Ctrl+Enter 生成、Tab 新建、Alt 拖动复制、Ctrl+0 适应、Space 平移、V 移动、H 抓手、Alt+Shift+F 整理、Ctrl+Shift+G 解组。Tab 已实际验证；其余不能仅凭列表称实操通过。
- 新建视频卡片有 5 分钟超长/首尾帧/首帧尝试入口。编辑器顶部参考/标记/特效/角色库/运镜，下部模型、生成模式、集中规格设置和高级开关。模式弹层显示文生视频可用；无参考素材时全能参考、图生视频、首尾帧、图片参考禁用。本轮未生成视频。

### 实际图片生成与人像工具（后续实操，覆盖此前“尚未提交”）

- 新建图片 5，LibImage，1:1、标准、2K、1 张，输入窗边青绿色陶杯描述后按 Ctrl+Enter。原节点内回填 2048×2048 结果。积分从 94 到 76，消耗 18；未提交视频、未购买会员。中间动画未及时捕获，不能称已观察等待全过程。
- 成功图片工具条有：人像、全景、多角度、打光、九宫格、高清、元素编辑、图层分离、九宫格切分等。尚未逐一实操，不以工具名称当成功证据。
- 点击人像菜单→人像调节：立即创建并连线到“人像质感调节”空结果节点；没有启动生成。源图片保留，编号缩略图位于新节点的浮动编辑器顶部。
- 人像工具在提示词区内显示带设置图标的功能标签。点击后展开五组参数，每组 3 档：人景融合（轻度对齐/自然融合/深度融合）、光影融合（柔和补光/自然匹配/氛围强化）、皮肤（清透修饰/自然肤质/真实肌理）、纹理（柔和纹理/自然纹理/颗粒质感）、锐度（柔焦/标准清晰/高清锐化）。已点击真实肌理，需进一步回读持久性；没有提交该效果生成。
- 音频节点为 SeedAudio 1.0，速度/音调/音量；输出设置包含中英、8/16/24/48 kHz、wav/mp3/pcm/ogg_opus。是可用控件观察，不等同于实际语音生成验收。
- CreateMore 本轮改动：图像专用工具先创建连接草稿、保留来源切换及真正映射的参数，再由卡片生成；参数收进功能标签。没有照搬本地工作流不支持的五组人像参数。局部交互样板仍需用户对照确认，不代表已完整复刻。
- Ctrl+0：先点空白画布，再按键，截图确认由 50% 调整至 89% 并适应节点；焦点在编辑器时那次没有生效，不能混记为成功。
- 从空白处拖出选框后出现批量工具栏：保存到资产、创建副本、批量下载、打组、添加到 Chat、创建为 Skill。进一步点击打组时控件引用失效；刷新后多选工具栏已消失，未把分组功能记作验证通过。
- 用户明确要求本地工作流、编辑映射直接可见。v0.4.1 将它们放在选中本地卡片编辑器顶部；卡片工具保持功能标签入口。此为已确认的本地差异，不是 LibTV 原有功能。
