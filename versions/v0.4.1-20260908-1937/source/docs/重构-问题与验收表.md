# 问题与验收表

基线：v0.4.0-20260908080910，已有独立备份和远端源码，重构期间不覆盖该包。

| ID | 问题与证据 | 当前结论 | 验收 |
|---|---|---|---|
| P01 | 最近本地打光、高清、音频任务报 fetch failed，冻结连接中 installDir/pythonPath 为空；用户截图再次确认当前配置界面路径为空、参数为 []；当前磁盘配置完整；8188 未监听 | 原生 Node 与打包 Electron 的只读配置加载均成功，尚未重现当前窗口为何为空；已请用户取消配置并正常重开以区分运行态与启动问题，不覆盖磁盘配置 | 待正式 EXE 冷启动→真实生成 |
| P02 | 图片卡片两参考图搭配 image-zimage，报参考素材未绑定 | 卡片可提交与工作流能力不一致，缺少提交前明确匹配 | 待两参考编辑、单参考和无参考分别验收 |
| P03 | 用户报告映射打不开；运行态与磁盘配置可能不一致 | 不把增加打开按钮视为映射交互完成 | 待真实图加载、选参映射、保存、生成、重开 |
| P04 | 新 LibTV 页面读取成功，关闭助手后合并读取超时；重新读取确认关闭成功。随后分开点击与读取，添加文本节点及进入手动编辑均成功，截图已查看 | 单次动作约 22 秒、读取约 43 秒，合并调用会超时；可以后台逐项观察，不抢桌面鼠标 | 后台点击→状态回读已通过，拖动等尚待 |

已检查不是已修复；脚本模拟通过不是正式 EXE 生成通过。未通过条目保留，不用数字汇总掩盖。

## P01 追加排查

- 用户正常重开仍报告空值。再次核对运行进程为正式 `v0.4.0-20260908080910/Windows-app/CreateMore.exe`，不是另一个版本；进程属于同一 Windows 用户。
- 只读 Node 和打包 Electron 加载器均可加载同一文件；配置 JSON 无 BOM，文件 ACL 与任务文件一致。未据此推断故障原因。
- 新诊断构建（不是修复版）`v0.4.0-20260908091558` 通过完整启动、桌面 IPC 和配置表单读取，后台启动时路径完整；报告 `.test-output/config-diagnostic.json`。
- 请求用户退出应用后运行根目录“检查连接配置.cmd”，比较用户手动启动的结果。此前后台诊断结果不能代替这次结果，需核对报告时间。
- 新增的诊断参数仅读取配置、打开隐藏诊断表单并退出，不调用配置保存或提交生成。诊断期间软件本身正常初始化日志/任务服务，未操作用户工程。
- 构建会改变根目录快捷方式；本轮诊断后将正常启动入口恢复到上一正式版，诊断使用独立 CMD，不能把中间诊断构建当作修复发布。

### 已复现的启动环境差异

用户手动启动诊断的报告时间为 2026-09-08 09:24:06 UTC。同一字符串路径 `AppData/Roaming/CreateMore/providers.json`，用户启动读到 178 字节，SHA256 `76d51b63d7d39b004d815f9741e70809ce71977ac23590c082a344e02c57350a`，启动路径为空；后台读到 518 字节，SHA256 `2482cda536b70d3fdee07a683699ff5989284c780e0cb253962e16cd391e3527`，路径完整。独立 Node 文件读取与正式 Electron 表单均在用户启动环境复现空配置。

这确认了前期后台配置/验收不等价于用户实际运行环境；底层为何呈现不同文件内容尚未查明，不擅自称为某个 Windows 机制。直接故障是用户实际配置缺少启动路径、8188 未运行；修复必须在用户启动环境执行，并重新核对该环境模型与节点是否可见。

“修复本机生成连接.cmd”用户运行成功（09:27 UTC）；配置备份完成，路径恢复，原后台就绪且 1662 个节点可见，五个图像模型及语音节点文件存在。此次只验证连接，未提交生成；自有空闲后台已停止。证据：`testing-output/user-comfy-repair.json`。

### 实际生成验收与动态输入误判（2026-09-08）

- 用户运行“验证本机生成.cmd”后，实际打包程序任务 `task-3e067f72-60f2-4629-8ade-ab64b8dc80e6` 失败，未进入生成；路径配置完整，失败后自有后台已停止。
- 用户随后点击启动后台，实时 8188 已可访问：ComfyUI 0.34.3、Python 3.13.12、PyTorch 2.12.1+cu130。未改动环境。
- 对失败任务的冻结工作流重新检查，唯一问题是节点 85 BatchImagesNode 的 `images` 被误报缺失；实际输入为 `images.image0`、`images.image1`，属于 Comfy V3 Autogrow 展开格式。
- 依据本机 ComfyUI `_io.py` 的 Autogrow 实现，源代码检查器已支持 prefix / names 模板展开，仍检查必需子输入；错误消息增加具体节点与字段。61 个 provider 测试通过，失败快照对实时节点注册表重新检查通过。
- 仅为预检查修复，尚未打包发布，不能宣称真实生成成功。失败快照与源代码默认 image-zimage 不同，需继续核对打包时保留的工作流资源，不覆盖用户编辑。原生映射和图片工具验收尚未执行。

### 后续已完成检查（本节覆盖上述待验收状态）

- 已确认打包图片默认版本 `767dd9926c5495117c6bf38c` 实为 38 节点 H3 视频图，输出误指向 CLIPLoader 41。候选构建 `v0.4.0-20260908095022` 中保留全部旧版本和 recovery 记录，仅切换回正确图片默认 `77a4365cf053f70a3422ddd1`；旧正式版及用户工程未改。
- 候选 EXE 后台启动、复用用户启动的真实 8188：图片卡片真实出图、AAA 图片尺寸工具、原生映射加载回读、工程保存重开通过。证据位置 `testing-output/background-local-check-location.json`；不是用户鼠标启动验收，报告原 scope 字样中的 user-launched configuration 不应理解成此次 EXE 由用户启动。
- 输出图片已实际查看，是窗边青绿色陶杯。图片 SHA256 为 `a2dac4b7e0d791212c1a71e72fef111a2b48a6f0bf5739ad1150e0c7c516b629`。
- 源码及候选版各 20 份默认工作流通过实时注册表结构审计：`testing-output/workflow-schema-audit.json`。不是 20 份真实生成通过。
- 本轮源代码服务通路实跑 7 项通过：图片生成、尺寸调整、裁切、高清、反推、文字生成、图生图；证据 `testing-output/local-cards-1788862024130/report.json`。图生图只是执行通路通过，不代表编辑质量最优。
- 原生映射增加公开控件预览和导入图身份检查。真实 ComfyUI 前端中选择采样节点、修改 steps、同步并验证预览通过：`testing-output/native-mapping-1788861883065/report.json`。仍需正式界面保存闭环与用户交互验收。
- 语音设计、定制音色、克隆均实际生成文件：`testing-output/qwen-speech-1788862113795/report.json`。未完成听审，不宣称情绪或音色相似度合格。
- 回归曾因模拟注册表未声明 output_node 而失败，改为对明确声明 false 的节点拦截；相关 87 项通过。全量回归仍须重新跑。
- 图片专用 Qwen 工具正在实跑；尚未将候选版设为正常启动入口或同步 GitHub。

### 2026-09-08 后续真实输出与交互补测

- `testing-output/local-cards-1788864300593/report.json`：全部 10 项通过，包含约 5 秒带音轨文生视频、图生视频、环境音；复用用户的 8188（owned=false），未新增或降级 ComfyUI。该报告是服务通路，不是全部真实界面操作。
- Qwen 专用工具：`qwen-images-1788862375264` 人像/打光/遮罩成功，遮罩外最大像素差为 0；`qwen-images-1788863267752` 多角度/九宫格成功，已看九宫格为 3×3 独立镜头。执行成功不意味着最优艺术效果。
- 新增单参考、双参考指令编辑工作流，不再把通用 ZImage 重绘称作精确编辑；真实队列输出见 `image-inputs-1788864167473/report.json`。旧默认版本全部保留，新流程独立 ID。
- 原生映射以 `native-mapping-1788864853101/report.json` 为本轮最新证据：实际 ComfyUI 前端、真实节点参数、公开控件预览、原生同步按钮、上下文校验通过，拒绝其他窗口发送同步。截图无 splash。早先 `native-mapping-1788861883065` 的截图仍有启动层，不应用它作为视觉成功证据。
- 新增图片工具准备入口：选择人像/多角度/打光/九宫格/高清只创建连接草稿，不提交任务；走普通卡片生成管线回填同一节点。保留 Image2 与本地两种来源，不自动付费回退。
- 实际隐藏 Electron 窗口鼠标测试：`tool-ui-1788865307988` 高清通过；该次打光因测试保护器拒绝 Image2 而失败，没有提交外部生成。已补保留本地工作流 ID；后一次测试仅分发 change 漏掉 input，未实际切换来源，不算工作流失败。
- `tool-ui-1788865493908/report.json`：真实工具按钮→草稿→选择本地来源→生成→同卡片回填，打光通过。全部工具实际界面回归继续进行。
- 源代码全量测试与脚本语法检查通过；后续有新增 UI 修改，打包前需再跑针对性界面回归。现有正常启动入口仍指向已冻结版本，候选未发布。

### 完整工具回归与用户位置确认

- `testing-output/tool-ui-1788865565519/report.json`：24/24 工具通过，真实隐藏 Electron 窗口按钮、标注鼠标、实际 IPC、现有 8188、本地结果与卡片回写。没有失败、实验跳过或外部生成。照片效果质量仍需单独评估。
- `testing-output/tool-ui-1788866085215/report.json`：语音设计、预设音色情绪、参考音色克隆 3/3 卡片界面通过，结果返回原卡片。参考为本轮合成语音；未完成听审。
- `dist/v0.4.0-20260908111106/Windows-app/.test-output/user-local-1788865982156/report.json`：正式 EXE 实际图片生成、AAA 尺寸工具、原生 GUI→API、映射校验保存并绑定、再次实际生成、工程重开保留映射版本及结果均通过。新自定义资源写验收目录，不修改用户库；现有用户后台保留运行。
- `v0.4.0-20260908110714` 源码/候选 EXE 的 36 项界面回归均通过，包含工具先创建草稿、不提前执行、生成回填同一草稿。后续用户要求可见入口，已修改，需重跑候选界面检查。
- 用户明确否决把本地工作流/映射收进菜单：改为本地卡片（包括工具卡片）上直接可见。编辑普通卡片映射绑定到此卡片独立版本，不覆盖本机默认资源。正式启用前继续针对性回归。
